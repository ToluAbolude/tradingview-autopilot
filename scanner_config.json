{
  "version": 2,
  "updated": "2026-08-17",
  "notes": "MINIMAL VM config — strategy trim only. Deliberately omits scoring/inst_profiles/ema_overrides: the VM has run on code defaults since the migration, and the repo config's inst_profiles are stale 15M-ATR-era tuning that would regress the H1-era SL/TP profiles hard-coded in setup_finder. Only add keys here that are meant to OVERRIDE code defaults.",
  "_trim_note": "2026-08-17: disabled_strategies switches votes OFF without deleting code (re-enable by removing from the list). Disabled: K (pattern-in-vacuum ~coin-flip; Z supersedes it AT zones), R/BB/O (mean-reversion & fib votes that fire both directions — 24% of signal-bars emitted long AND short at once; fib entries net-negative per fib_backtest), H (weak), U (proximity, direction-blind), P (superseded by the hard TRADE_WINDOWS gate in daily_plan_gate). class_vote_overrides drops zone votes on indices (their inst profiles already say indices gap through zones). pass1_min_score 6 -> 5 compensates the removed vote supply.",
  "disabled_strategies": ["K", "R", "BB", "O", "H", "U", "P"],
  "class_vote_overrides": { "index": ["C", "C-near", "F"] },
  "thresholds": { "pass1_min_score": 5 }
}
